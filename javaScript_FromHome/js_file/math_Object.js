// math objects in java Script
console.log('Welcome');

let z = Math;       // Give the function inside the math.
let a = Math.PI;    // Give the value of pi.
let b = Math.E;     // Give the value of e.
let c = Math.round(5.6);    //Give the round value of number.
let d =Math.ceil(5.3);    //Give the maximum value e.g. 5.3-->6,  5.6-->6.
let e = Math.floor(5.3);    //GIve the minimum value.
let f = Math.abs(-5);   //Give the absolute value.
let g = Math.sqrt(5);   //Give the square value.
let h = Math.pow(2,3);  //It mean 2^3.
let i = Math.min(21,5,64,988,356);  //Give the minimum value.
let j = Math.max(21,5,64,988,356);  //Give the maximum value.
let k = Math.random();  //Give the random number.
let l = 100*Math.random();  //Give the random number between 1-100;
let m = 50+(100-50)*Math.random();   //Give the random value 50-100;
let n = Math.ceil(100*Math.random());   //Give the random value from 1-100 (no decimal). 


console.log(m);
console.log(e);
console.log(d);
console.log(c);
console.log(b);
console.log(z);
console.log(a);